"""Unit tests for the backend card-generation pipeline.

The tests are written against the stdlib ``unittest`` framework so they run
without any extra dependency, but they also auto-discover under ``pytest``.

Run them directly::

    cd backend && python -m unittest discover -s tests -t .

Or via the project root::

    npm run test:unit
"""

__all__: list[str] = []
