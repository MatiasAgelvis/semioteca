[pytest]
# Make backend modules importable as top-level names (``verify_cards``,
# ``generate_cards_json``) so the test files can keep their short imports.
pythonpath = .
testpaths = tests
addopts = -ra
