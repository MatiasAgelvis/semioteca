[tools]
node = "lts"
python = "3.14"

# `uv` manages its own virtualenv under `backend/.venv`. Mise still pins the
# Python interpreter version; uv finds it and creates the venv on first
# `uv sync`.
