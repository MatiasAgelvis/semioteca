/**
 * Theme names in `frontend/src/routes/layout.css`. Those names are chosen to be
 * generic (`light`/`dark`) so application code never speaks daisyUI's theme
 * taxonomy — the palette lives entirely in the CSS theme blocks.
 */
export const LIGHT_THEME = 'light';
export const DARK_THEME = 'dark';
export const THEME_STORAGE_KEY = 'theme';
